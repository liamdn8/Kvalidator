apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kvalidator-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: kvalidator.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: kvalidator
            port:
              number: 80
